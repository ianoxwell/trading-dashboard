export const environment = {
  production: false,
  tradeProcessingTime: {
    min: 0,   // No delay for tests
    max: 100  // Minimal delay for tests (100ms)
  },
  newItemTimerDuration: 1000 // 1 sec for tests
};
